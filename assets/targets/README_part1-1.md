# MindAR target สำหรับแผน 1.1

หน้านี้ต้องใช้ไฟล์นี้:

```text
assets/targets/part1-1.mind
```

ให้สร้างไฟล์ `.mind` จากภาพในโฟลเดอร์:

```text
assets/images/part1/1.1/targets/
```

เรียงภาพตามชื่อไฟล์ 01_target.jpg ถึง 20_target.jpg แล้วอัปโหลดไฟล์ที่ได้เป็น:

```text
assets/targets/part1-1.mind
```

ถ้าไม่มีไฟล์นี้ หน้าเว็บจะเปิดได้ แต่จะสแกนภาพไม่เจอ
